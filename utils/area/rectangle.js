const testing = require("./circle");

function calculateRectangleleArea(width, height) {
  return width * height;
}

// to export a single function or variable
module.exports = calculateRectangleleArea;
